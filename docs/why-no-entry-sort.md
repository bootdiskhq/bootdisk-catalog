# Source entry ordering

The curation and presentation projections preserve ingest-manifest entry order.
They do not reinterpret identifiers such as `K1`, `K24` and `K38` as numeric sort
keys. Source order can be meaningful evidence, while presentation order is a UI
concern that can be changed without mutating the underlying observation.
